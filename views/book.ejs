<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <link rel="stylesheet" href="styles/style.css">
    <title>Book Details</title>
    <style>
        body {
            margin: 0;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background-color: #f9f9f9;
        }

        .book-card {
            max-width: 800px;
            margin: 20px auto;
            padding: 20px;
            background-color: #fff;
            border: 2px solid #ddd;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }

        .book-cover {
            max-width: 100%;
            border-radius: 8px;
        }

        .book-details {
            margin-top: 20px;
            display: flex;
            flex-direction: column;
            gap: 10px;
        }

        .book-description {
            margin-top: 20px;
            font-size: 16px;
            line-height: 1.6;
        }

        @media (max-width: 768px) {
            .book-details {
                margin-top: 10px;
            }

            .book-description {
                margin-top: 10px;
            }
        }
    </style>
</head>
<body>
    <%- include('header.ejs') %>
    <div class="container">
        <div class="book-card">
                <div class="row">
                    <div class="col-md-4">
                        <img src="<%= book.image_path %>" alt="Book Cover" class="book-cover">
                    </div>
                    <div class="col-md-8 book-details">
                        <h2><%= book.title %></h2>
                        <p><%= book.short_intro %></p>
                        <p><%= book.date_ %></p>
                        <div class="rating">
                            <% for( let i = 0; i < book.rating; i++ ) { %>
                                <span class="star">⭐</span>
                            <% } %>
                        </div>
                        <div class="edit-delete-buttons">
                            <form method="POST">
                                <input type="hidden" name="bookId" value="<%= book.id %>">
                                <button formaction="/edit" class="btn btn-primary">Edit</button>
                                <button formaction="/delete" class="btn btn-danger">Delete</button>
                            </form>
                         
                    </div>
                    </div>
                    
                </div>
    
                <div class="book-description">
                    <p><%= book.description %></p>
                </div>

        </div>
    </div>
    <%- include('footer.ejs') %>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL" crossorigin="anonymous"></script>
</body>
</html>
